@startuml
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

title React框架容器图

System_Boundary(react_boundary, "React核心") {
    Container(reconciler, "协调器(Reconciler)", "虚拟DOM差异计算")
    Container(scheduler, "调度器(Scheduler)", "任务优先级调度")
    Container(renderer, "渲染器(Renderer)", "平台特定渲染实现")
    Container(hooks, "Hooks系统", "状态和生命周期管理")
    Container(compiler, "React编译器", "优化组件代码")
}

System_Ext(dev_tools, "开发者工具", "React DevTools")
System_Ext(bundler, "打包工具", "Webpack/Vite等")

Rel(dev_tools, reconciler, "调试")
Rel(reconciler, scheduler, "调度更新")
Rel(reconciler, renderer, "提交更新")
Rel(reconciler, hooks, "管理状态")
Rel(compiler, reconciler, "优化组件")
Rel(bundler, react_boundary, "打包应用")

legend right
<b>核心模块</b>
1. 协调器: Fiber架构实现
2. 调度器: 并发模式基础
3. 渲染器: 平台特定实现
4. Hooks: 函数组件状态管理
endlegend

@enduml
